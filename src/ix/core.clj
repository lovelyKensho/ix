(ns ix.core
  (:gen-class))
;; --- The start ---

(def first-name "Alexander")
;; --- The end ---

;; --- Notes ---


(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
